import 'package:flutter/material.dart';

class HealthCornerScreen extends StatelessWidget {
  const HealthCornerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SizedBox(
        child: Column(
          children: const [],
        ),
      ),
    );
  }
}
